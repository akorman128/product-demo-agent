### Example environment variables for product-demo-agent
#
# Copy this to `.env` and fill in real values:
#   cp env.example .env
#
# NOTE: `.env` is already ignored by git via `.gitignore`

# Fanfix example (used by scripts/fanfix-send-message.yaml if you enable form auth)
FANFIX_EMAIL=
FANFIX_PASSWORD=

# Optional: AI narration
ANTHROPIC_API_KEY="your-api-key"


